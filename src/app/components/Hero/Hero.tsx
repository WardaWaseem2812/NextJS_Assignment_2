import styles from './Hero.module.css';

const Hero = () => {
  return (
    <section className={styles.hero}>
      <h2>Hero Section</h2>
      <p>This is the Hero Section</p>
    </section>
  );
};

export default Hero;

